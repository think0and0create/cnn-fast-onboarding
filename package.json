{
  "name": "cnn-fast-onboarding",
  "version": "0.1.0",
  "description": "项目驱动的 CNN 速成课",
  "private": true,
  "scripts": {
    "docs:sync": "bash scripts/sync-docs.sh",
    "docs:dev": "vuepress dev docs",
    "docs:build": "npm run docs:sync && vuepress build docs",
    "docs:preview": "vuepress preview docs"
  },
  "devDependencies": {
    "sass-embedded": "^1.100.0",
    "vuepress": "^2.0.0-rc.30"
  },
  "dependencies": {
    "@vuepress/bundler-vite": "^2.0.0-rc.30",
    "@vuepress/theme-default": "^2.0.0-rc.132"
  }
}
